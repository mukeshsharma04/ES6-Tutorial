// Promises are native in ES6 they represent an action that hasn't completed yet they allow us to add handlers for an success or failure
function getNumberFact(num) {
console.log(num);
}

 getNumberFact(4);
